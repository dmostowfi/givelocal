<%= form_with(model: @my_wish, local: true) do |f| %>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.hidden_field :gift_id, value: @gift.id %>

  <%= f.submit "Create my wish", class: "btn btn-block btn-outline-secondary" %>
<% end %>
